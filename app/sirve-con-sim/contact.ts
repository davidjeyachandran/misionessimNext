/**
 * Shared by the page, the enquiry form and the no-JS thank-you page, so the
 * address can't drift between them. The site had two different contact
 * addresses in circulation before this (`info@misionessim.org` in places where
 * live says `sim.preguntas@sim.org`).
 */
export const CONTACT_EMAIL = "sim.preguntas@sim.org";
export const WHATSAPP_URL = "https://chat.whatsapp.com/FfwZVyN09pA49SpPhyITWd";
