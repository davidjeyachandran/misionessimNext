# Domain docs

This is a single-context repository.

Before work that changes a domain term or a recorded decision, read:

- `CONTEXT.md` at the repository root.
- Relevant decisions in `docs/adr/`.

Use the glossary's vocabulary in tests, plans, and implementation notes. If a
term is missing, add it only when its meaning is settled.
