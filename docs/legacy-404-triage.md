# Legacy 404 triage — Google Search Console, 2026-08-24

Google's "Not found (404)" export for `misionessim.org`: 320 rows, **300 unique
paths** after deduping hosts and query strings. Frozen at
[`data/legacy-404s.json`](../data/legacy-404s.json) — Search Console prunes URLs
once it stops asking for them, so this list cannot be regenerated later.

## What the list actually is

Almost none of it is rebuild fallout. The paths are **Drupal-era**:
`/content/<slug>`, `/recurso/<slug>`, `/curso-vamos/<section>`,
`/images/<tema>_adjuntos/<file>`, `/phocadownload/`, `/sites/default/files/`.
WordPress never carried redirects for that space either, so these URLs have been
404ing for years. Search Console simply became visible again when the rebuild
made someone look. `Last crawled` is when Google last *tried*, not when the URL
last worked — Google retries a 404 for years.

## Disposition

| | Count |
|---|---|
| Already resolved before this change (Search Console data is stale) | 24 |
| Now redirected by `vercel.json` | 107 |
| Still 404 — document whose file did not survive | 99 |
| Still 404 — blog post that was never in the migration | 46 |
| Still 404 — junk / malformed | 9 |
| Other host (`mail.`, `www.`) | 15 |

### Now redirected (107)

Generated by `yarn build:legacy-redirects`, section 4, from
[`data/legacy-404s.json`](../data/legacy-404s.json) plus the hand-decided
[`data/legacy-page-map.json`](../data/legacy-page-map.json):

- **61 documents → `/recursos/<file>`.** The Drupal file space spelled one
  document a dozen ways (`Test_Dones_Espirituales.pdf`,
  `test_dones_espirituales.pdf`, `Pablo_Bajo_Estrés.pdf`), across four different
  directories. Vercel matches sources byte-for-byte, so each spelling gets its
  own exact rule, reunited with the one Contentful asset by an accent- and
  case-insensitive key.
- **26 magazine URLs → the edition page or its PDF.** Includes the Drupal
  magazine filenames (`vamosoct13.pdf`) that `pdfToSlug` still carries.
- **`/content/<slug>` → `/revistavamos/<slug>/`.** `/content/` was Drupal's alias
  for a magazine edition — the same slug space `/la-revista/` later used — so it
  gets the same passthrough wildcard, plus exact rules for the seven slugs that
  drifted before WordPress (`la-persecucion` → `persecucion`, `regresando-casa`
  → `regresando-a-casa`, …).
- **Drupal pages** → their counterpart: `/quienessomos/` → `/nosotros/`,
  `/contact/` → `/sirve-con-sim/`, `/ora-con-nosotros/` and `/ora-por-misiones/*`
  → `/ora/`, `/curso-vamos*` and `/recurso/*` → `/recursos/`, `/da-la-obra/` →
  `/` (donations are out of scope).

Two paths carry a literal space from a typo'd href in old content
(`/la-revista/ trabajo-en-equipo`). Every spaced source gets a percent-encoded
twin, since that is what Googlebot actually sends.

### Deliberately left to 404

**99 documents** from the old VAMOS course library — `/images/<tema>_adjuntos/`
and `/phocadownload/`. No Contentful asset carries the file, and the WordPress
probe on 2026-08-23 found nothing at `/sites/default/files/magazinefiles/`
either. The bytes are gone unless someone has a backup or they can be pulled
from the Wayback Machine. Listed by `yarn build:legacy-redirects`.

**46 blog posts**, clustered in months the migration has no posts for at all —
2024-08, 2024-09, 2025-01, 2025-02, plus two 2024-05 slugs. They are absent from
`export/posts/` (335 posts, matching WordPress's own `x-wp-total`), from
`export/import-plan.json`, and from sim-blog. By subject they look like
VAMOS-issue articles: a singleness cluster in 2024-09, an Africa and digital
cluster in 2025-01, a pre-departure cluster in 2025-02.

**9 junk paths** — `sim.preguntas@sim.org` and `www.jewishvoice.org` appended to
post URLs by broken hrefs in old content, `/wp-content/plugins/*`,
`/wp-json/`, an InDesign clipboard scratch file, a Joomla-era
`?option=com_content` URL.

Nothing here gets a redirect. Google treats a 301 onto an unrelated index page
as a soft 404 — it is the same signal as the 404, minus the honesty, plus a rule
to maintain forever. A 404 is the correct answer for content that is genuinely
gone; only a URL with a true equivalent earns a redirect.

## Not fixable in `vercel.json`

**`mail.misionessim.org` (13 paths).** A path rule cannot reach another
hostname. The record is proxied by Cloudflare (`104.21.83.253`,
`172.67.184.4`) to an origin that no longer terminates TLS, so HTTPS returns
**525** and HTTP returns 404. The 525 is the problem: Google treats 5xx as "try
again later" and keeps the URL queued indefinitely, where a 404 or a 301 would
settle it. The domain's MX points at the apex, not at `mail.`, so nothing about
mail delivery depends on this record — but check webmail before touching it.
Fix at Cloudflare, not here: a redirect rule sending `mail.misionessim.org/*` to
`https://misionessim.org/$1`, or delete the record.

**`www.misionessim.org` (2 paths).** Already correct — 308s to the apex. Those
two rows 404 only because the underlying path did; both are now covered.

## Known content bug

One post carries an uppercase slug: `/blog/2021-08/Discipulando-con-el-manual-vamos/`
is live, the lowercase form 404s. It is the only mixed-case slug among 899. The
redirect added for the legacy `/blog/2021-09/` form points at the live uppercase
URL and will need updating if the Contentful slug is fixed.
