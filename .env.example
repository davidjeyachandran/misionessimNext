# Copy to .env.local and fill in values. Never commit .env.local.

# Contentful — same space as mi-movilicemos (~/websites/poc/mi-movilicemos)
# Content Delivery API (read-only, published content) — used at build time and in route handlers
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=
CONTENTFUL_MANAGEMENT_TOKEN=

# Contentful on-demand revalidation — secret shared between this app and the Contentful webhook
CONTENTFUL_REVALIDATE_SECRET=

# Contact form (Sirve con SIM). The site is a static export, so the browser
# POSTs straight to a hosted form endpoint — Formspree (https://formspree.io/f/<id>)
# or Web3Forms (https://api.web3forms.com/submit) both accept a JSON POST.
# Web3Forms needs its access key in the body; Formspree carries the id in the URL
# and needs no key. Both are NEXT_PUBLIC_ (inlined at build time — a change needs
# a redeploy) and neither value is a secret. Leave the endpoint blank and the form
# is not rendered at all, only the email/WhatsApp links beside it.
NEXT_PUBLIC_CONTACT_ENDPOINT=
NEXT_PUBLIC_CONTACT_ACCESS_KEY=

# Resend — unused while the form posts to a hosted endpoint. Kept for the
# route-handler approach in docs/PROGRESS.md, which needs `output: "export"`
# dropped from next.config.ts first.
RESEND_API_KEY=
CONTACT_FORM_TO_EMAIL=

# App URL (used by sitemap.ts and metadataBase)
NEXT_PUBLIC_SITE_URL=https://misionessim.org
